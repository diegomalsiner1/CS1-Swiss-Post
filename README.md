# CS1-Swiss-Post
Repository for Case Study 
TESTFILE
TEST

Each Trafo input data is expected to be in Watts and have a dedicated worksheet, where the relevant column has "-avg[W]" in it's name. 
The Input data must be in the same general layout as the input_template.xlsx. This includes a config file which is read into the code at the very beginning.